#include <stdio.h>
int main(){
    // int a;
    // int r;
    // printf("Enter the radius :");
    // scanf("%d",&a);
    // r = (3.14*a*a);
    // printf("the volume of the given radius is %d",r);
    // int a =129;
    // char c=(char)a;
    // printf("%d",c);

    int c = 1000,b =15;
    float a = (float)c/b;
    printf("%f",a);
    
    return 0;
}